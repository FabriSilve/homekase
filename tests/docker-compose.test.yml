services:
  homekase-test:
    build:
      context: ..
      dockerfile: tests/Dockerfile.test
    container_name: homekase-test
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - CI=true
